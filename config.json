{
  "name": "HTML to PDF",
  "desc": "Action convert HTML to PDF.",
  "version": {
    "major": 1,
    "minor": 0
  },
  "actions": [
    {
      "name": "Convert HTML to PDF",
      "desc": "This action converts a webpage (via URL) or an HTML file into a well-formatted PDF document. Users can input a URL or upload an HTML file, and the action will capture the content, including text, images, and layout, transforming it into a PDF.",
      "category": "Conversion",
      "stdout": "",
      "stderr": "",
      "returnCodes": [
        0
      ],
      "configurations": [
        {
          "name": "Convert HTML via URL to PDF.",
          "desc": "This action converts a webpage (via URL) or an HTML file into a well-formatted PDF document. Users can input a URL, and the action will capture the content, including text, images, and layout, transforming it into a PDF.",
          "program": "docker run --platform linux/amd64 -v ${WORKING_DIRECTORY}:/data -w /data --rm pdfix/html-to-pdf:latest html-to-pdf -i ${URL} -o $(basename ${OUTPUT_PDF}) --name ${LICENSE_NAME} --key ${LICENSE_KEY}",
          "args": [
            "---url",
            "${INPUT_URL}",
            "--output",
            "${OUTPUT_PDF}"
          ]
        },
        {
          "name": "Convert HTML file to PDF.",
          "desc": "This action converts a webpage (via URL) or an HTML file into a well-formatted PDF document. Users can input an HTML file, and the action will transform it into a PDF.",
          "program": "docker run --platform linux/amd64 -v ${WORKING_DIRECTORY}:/data -w /data --rm pdfix/html-to-pdf:latest html-to-pdf -i $(basename ${INPUT_HTML}) -o $(basename ${OUTPUT_PDF}) --name ${LICENSE_NAME} --key ${LICENSE_KEY}",
          "args": [
            "--input",
            "${INPUT_HTML}",
            "--output",
            "${OUTPUT_PDF}"
          ]
        }
      ],
      "args": [
        {
          "name": "URL",
          "desc": "URL address.",
          "flags": 2,
          "type": "url",
          "value": ""
        },
        {
          "name": "INPUT_HTML",
          "desc": "HTML file.",
          "flags": 2,
          "type": "file_path",
          "ext": "html",
          "value": ""
        },
        {
          "name": "OUTPUT_PDF",
          "desc": "Output PDF file.",
          "flags": 4,
          "type": "file_path",
          "ext": "pdf",
          "value": ""
        }
      ]
    }
  ]
}